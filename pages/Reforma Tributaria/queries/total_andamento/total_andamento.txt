SELECT
  COUNT(cliente) AS total_andamento
FROM public.cliente
WHERE COALESCE(NULLIF(status2, ''), status) IN (
    'Aguardando Validação do cliente',
    'Aguardando retorno do cliente',
    'Aguardando Validação Interna'
  );