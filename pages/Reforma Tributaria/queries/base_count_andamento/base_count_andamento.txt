SELECT
  COUNT(cliente) AS total_clientes
FROM public.cliente
WHERE setor = 'Base'
  AND COALESCE(NULLIF(status2, ''), status) IN (
    'Aguardando Validação do cliente',
    'Aguardando retorno do cliente',
    'Aguardando Validação Interna'
  );