SELECT
  count(id_planilha) sem_retorno
FROM public.cliente
WHERE
  COALESCE(NULLIF(apresentacao, ''), status) = 'Aguardando retorno do cliente'
	AND data_apresentacao_planilha_cliente IS NULL
  AND chamado IS NOT NULL;